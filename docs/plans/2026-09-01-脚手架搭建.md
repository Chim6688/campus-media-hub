# 项目脚手架搭建实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 搭建"推文工作流"项目骨架：Vue3 前端 + Netlify Functions 后端 + 可本地验证的最小闭环。

**Architecture:** 单仓库，`frontend/` 为 Vue3 + Vite 静态站，`backend/functions/` 为 Netlify Functions（v2 风格 .mjs），`netlify.toml` 统一编排；`/api/*` 重写到函数。数据层本期用内存占位，Supabase 在后续任务接入（预留 `store` 抽象点）。

**Tech Stack:** Vue3、Vite 6、Netlify Functions v2（Node ESM）

## Global Constraints

- 目录规范：前端代码只在 `frontend/`，后端代码只在 `backend/`，文档在 `docs/`
- AI Key 等机密一律走环境变量，禁止出现在任何代码文件中
- 每个任务完成后独立 git 提交，提交信息格式 `<type>: <中文描述>`
- 注释使用中文

---

### Task 1: 根配置文件

**Files:**
- Create: `.gitignore`
- Create: `netlify.toml`

**Interfaces:**
- Produces: `/api/*` 统一后端路由约定（前端所有请求走此前缀）

- [x] **Step 1: 写 `.gitignore`**

```gitignore
node_modules/
dist/
.netlify/
.env
.env.*
*.local
```

- [x] **Step 2: 写 `netlify.toml`**

```toml
[build]
  publish = "frontend/dist"
  functions = "backend/functions"
  command = "cd frontend && npm run build"

# 前端统一通过 /api/* 访问后端函数
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
```

- [x] **Step 3: 提交**

```bash
git add .gitignore netlify.toml
git commit -m "chore: 根配置（netlify 编排与 gitignore）"
```

---

### Task 2: 后端函数骨架（backend/functions）

**Files:**
- Create: `backend/functions/verify.mjs`
- Create: `backend/functions/tasks.mjs`
- Create: `backend/functions/package.json`

**Interfaces:**
- Produces: `GET /api/tasks` 返回 `{ tasks: [...] }`；`POST /api/tasks`（body: `{theme, type, author}`）创建任务；`POST /api/verify`（body: `{code}`）校验口令
- Produces: 口令从环境变量 `ACCESS_CODE` 读取（本地默认 `demo2026`，仅开发用）

- [x] **Step 1: 写 `backend/functions/package.json`**

```json
{
  "name": "tweet-workflow-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module"
}
```

- [x] **Step 2: 写 `backend/functions/verify.mjs`（口令校验）**

```js
// 共享口令校验：保护 AI 额度，口令由管理者配置在环境变量 ACCESS_CODE
export default async (req) => {
  if (req.method !== 'POST') {
    return new Response(JSON.stringify({ error: '仅支持 POST' }), { status: 405 });
  }
  const { code } = await req.json();
  const expected = process.env.ACCESS_CODE || 'demo2026'; // 本地开发默认值
  if (code === expected) {
    return new Response(JSON.stringify({ ok: true }), {
      headers: { 'Content-Type': 'application/json' },
    });
  }
  return new Response(JSON.stringify({ ok: false, error: '口令错误' }), { status: 401 });
};
```

- [x] **Step 3: 写 `backend/functions/tasks.mjs`（任务 CRUD 占位）**

```js
// 任务管理：当前为内存占位实现，Supabase 接入后仅替换 store 部分，接口不变
let seq = 0;
const tasks = [];

export default async (req) => {
  const headers = { 'Content-Type': 'application/json' };

  // GET：任务列表
  if (req.method === 'GET') {
    return new Response(JSON.stringify({ tasks }), { headers });
  }

  // POST：新建任务
  if (req.method === 'POST') {
    const { theme, type, author } = await req.json();
    if (!theme || !author) {
      return new Response(JSON.stringify({ error: 'theme 和 author 必填' }), { status: 400, headers });
    }
    const task = {
      id: ++seq,
      theme,
      type: type || '活动报道',
      author,
      status: 'writing', // writing -> typesetting -> reviewing -> published
      content: '',
      createdAt: new Date().toISOString(),
    };
    tasks.push(task);
    return new Response(JSON.stringify({ task }), { status: 201, headers });
  }

  return new Response(JSON.stringify({ error: '不支持的方法' }), { status: 405, headers });
};
```

- [x] **Step 4: 语法验证**

```bash
node --check backend/functions/verify.mjs
node --check backend/functions/tasks.mjs
```

预期：无输出（语法通过）

- [x] **Step 5: 提交**

```bash
git add backend/functions
git commit -m "feat: 后端函数骨架（口令校验与任务 CRUD 占位）"
```

---

### Task 3: 前端骨架（frontend）

**Files:**
- Create: `frontend/package.json`
- Create: `frontend/vite.config.js`
- Create: `frontend/index.html`
- Create: `frontend/src/main.js`
- Create: `frontend/src/App.vue`
- Create: `frontend/src/api/client.js`

**Interfaces:**
- Consumes: Task 2 的 `/api/verify`、`/api/tasks` 接口
- Produces: `client.js` 导出 `request(path, options)`——自动附带 `X-Access-Code` 请求头（口令存 localStorage）

- [x] **Step 1: 写 `frontend/package.json`**

```json
{
  "name": "tweet-workflow-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.3.5"
  }
}
```

- [x] **Step 2: 写 `frontend/vite.config.js`**

```js
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';

// 本地开发时把 /api 代理到 netlify dev 的默认端口 8888
export default defineConfig({
  plugins: [vue()],
  server: {
    proxy: {
      '/api': 'http://localhost:8888',
    },
  },
});
```

- [x] **Step 3: 写 `frontend/index.html`**

```html
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>推文工作流</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.js"></script>
  </body>
</html>
```

- [x] **Step 4: 写 `frontend/src/main.js`**

```js
import { createApp } from 'vue';
import App from './App.vue';

createApp(App).mount('#app');
```

- [x] **Step 5: 写 `frontend/src/api/client.js`**

```js
// 统一请求封装：自动携带共享口令（口令门用，非账号体系）
export async function request(path, options = {}) {
  const code = localStorage.getItem('accessCode') || '';
  const res = await fetch(path, {
    ...options,
    headers: {
      'Content-Type': 'application/json',
      'X-Access-Code': code,
      ...(options.headers || {}),
    },
  });
  const data = await res.json().catch(() => ({}));
  if (!res.ok) {
    throw new Error(data.error || `请求失败（${res.status}）`);
  }
  return data;
}
```

- [x] **Step 6: 写 `frontend/src/App.vue`**

```vue
<script setup>
import { onMounted, ref } from 'vue';
import { request } from './api/client.js';

const tasks = ref([]);
const error = ref('');
const code = ref(localStorage.getItem('accessCode') || '');
const verified = ref(false);
const theme = ref('');
const author = ref('');

// 校验口令：通过后加载任务列表
async function verify() {
  error.value = '';
  try {
    await request('/api/verify', { method: 'POST', body: JSON.stringify({ code: code.value }) });
    localStorage.setItem('accessCode', code.value);
    verified.value = true;
    await loadTasks();
  } catch (e) {
    error.value = e.message;
  }
}

async function loadTasks() {
  try {
    const data = await request('/api/tasks');
    tasks.value = data.tasks;
  } catch (e) {
    error.value = e.message;
  }
}

// 新建任务（脚手架演示用，后续移入独立表单页）
async function createTask() {
  error.value = '';
  try {
    await request('/api/tasks', {
      method: 'POST',
      body: JSON.stringify({ theme: theme.value, author: author.value }),
    });
    theme.value = '';
    await loadTasks();
  } catch (e) {
    error.value = e.message;
  }
}

onMounted(() => {
  if (code.value) verify();
});
</script>

<template>
  <main class="page">
    <h1>推文工作流</h1>

    <!-- 口令门 -->
    <section v-if="!verified" class="gate">
      <p>请输入站点口令</p>
      <input v-model="code" placeholder="站点口令" @keyup.enter="verify" />
      <button @click="verify">进入</button>
    </section>

    <!-- 任务区 -->
    <section v-else>
      <div class="new-task">
        <input v-model="theme" placeholder="推文主题，如：迎新晚会报道" />
        <input v-model="author" placeholder="你的名字" />
        <button @click="createTask">新建任务</button>
      </div>
      <ul class="task-list">
        <li v-for="t in tasks" :key="t.id">
          【{{ t.type }}】{{ t.theme }} — {{ t.author }}（{{ t.status }}）
        </li>
      </ul>
      <p v-if="tasks.length === 0">暂无任务，新建一个吧</p>
    </section>

    <p v-if="error" class="error">{{ error }}</p>
  </main>
</template>

<style scoped>
.page { max-width: 640px; margin: 40px auto; font-family: system-ui, sans-serif; }
.gate input, .new-task input { margin-right: 8px; padding: 6px 10px; }
.task-list li { padding: 8px 0; border-bottom: 1px solid #eee; }
.error { color: #c0392b; }
</style>
```

- [x] **Step 7: 安装依赖并构建验证**

```bash
cd frontend; npm install; npm run build
```

预期：构建成功，产出 `frontend/dist/`

- [x] **Step 8: 提交**

```bash
git add frontend
git commit -m "feat: Vue3 前端骨架（口令门 + 任务列表最小闭环）"
```
