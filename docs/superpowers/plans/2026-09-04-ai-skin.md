# AI 描述生成皮肤 · 实施计划（B 批 / 模板生产线第二级）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 小编输入一句风格描述（如「蓝金科技感」）→ AI 生成整套配色 → 一键应用到当前任务预览，复用既有自动保存与 layout_theme 持久化，实现模板生产线「浏览→选择→创造」闭环。

**Architecture:** 前端 `skin.js` 纯函数定义 8 个配色键契约 + `normalizeSkin` 校验清洗（AI 输出不可信，字段过滤 + hex 校验）；后端 `prompts.mjs` 加一条 `gen_skin` action（字典分发，ai.mjs 零改动）；TaskDetail 加「✨ AI 配色」弹窗，应用 = 清空 themeOverrides 后写入 8 个色键 → 预览 computed 即时刷新，`themeSnapshot` watch 触发防抖自动保存，任务级 `layout_theme` 持久化，**零后端接口改动、零 schema 改动**。

**Tech Stack:** Vue 3 / Netlify Functions / DeepSeek(主力)+智谱(fallback) / node --test（无新依赖）

## Global Constraints（红线，每个任务隐含遵守）

1. 「复制到公众号」机制不动；预览与复制同源（wechatHTML computed）
2. AI 生成的是**配色**（8 个 hex）；圆角/字号/间距仍由用户用既有参数面板手调（AI 不碰数值令牌，防止破坏性布局）
3. `resolveTheme(themeId, overrides)` 语义不变：overrides 可包含任意令牌键（含面板未暴露的 pageBg/ink 等），合并顺序 TOKEN_DEFAULTS → 预设 → overrides
4. 应用 AI 皮肤前先清空既有 overrides（AI 皮肤是完整配色方案，与切预设清覆盖的既有行为一致）
5. 既有 15 项前端测试 + 3 项后端测试必须保持全绿（本批纯增量）
6. 注释规范：新代码主要片段带简明中文注释
7. PowerShell 5.1：不支持 `&&`（用 `;`）；**同一文件多处 Edit 必须串行执行**（并行编辑会互相覆盖，批 1 已踩坑）
8. 每任务 TDD；组件改动主会话浏览器实测；部署后用户线上验收

## 现状关键事实（执行者必读，已核对）

- `frontend/src/utils/themes.js`：`resolveTheme` 纯展开合并；皮肤对象含 8 个色键：`pageBg/accentA/accentB/ink/cardBg/cream/creamBorder/creamText`
- `frontend/src/components/TaskDetail.vue`：
  - L504-514：`themeOverrides` reactive + `OVERRIDES_SCHEMA`（6 键：accentA/accentB/radius/titleFontSize/bodyFontSize/sectionGap）
  - L516-521：`watch(themeId)` 切预设清空覆盖（switching 保护）
  - L524-526：`themeSnapshot` watch → `scheduleAutoSave()`（防抖 2s）
  - L528-531：`wechatHTML` computed 传 `{...themeOverrides}`
  - L240-253：`callAI(action, payload)` 统一封装（aiLoading + aiElapsed 计时已接好）
  - L700-712：预览头部按钮区（select → 🖼 画廊 → 🎨 调参数 → 📋 复制）
  - 弹窗先例：rejectModal（L840 附近）`modal-mask` + `modal` 样式类已有
  - AI 返回剥代码块兜底先例：`text.replace(/```json\s*/g,'').replace(/```\s*/g,'').trim()`
- `backend/functions/lib/prompts.mjs`：`PROMPTS` 字典，每条 `(p) => [{role,content}...]`；`ai.mjs` 按 action 分发，未命中 400
- `frontend/package.json` test script 收录 `tests/*.test.mjs`；`backend/functions/package.json` 同
- 皮肤色键的渲染用途（prompt 里要告诉 AI）：pageBg=页面底色(浅)、accentA=标题错位层/序号/金句描边、accentB=胶囊/圆点/菱形、ink=正文与描边(深)、cardBg=卡片底(近白)、cream/creamBorder/creamText=落款卡三件套(浅/中/灰)

---

## Task 1: skin.js 纯函数——配色契约 + normalizeSkin 清洗（TDD）

**Files:**
- Create: `frontend/src/utils/skin.js`
- Create: `frontend/tests/skin.test.mjs`

**Interfaces:**
- Produces: `SKIN_FIELDS`（`[{key, label, hint}]` 8 项，Task 3 弹窗与后端 prompt 语义对齐用）；`normalizeSkin(raw)` → `object`（只保留合法色键；Task 3 依赖此签名）

- [ ] **Step 1: 写失败测试**

创建 `frontend/tests/skin.test.mjs`：

```js
// AI 生成皮肤的配色契约与清洗（B 批）：AI 输出不可信，字段过滤+hex校验是唯一防线
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { normalizeSkin, SKIN_FIELDS } from '../src/utils/skin.js';

test('SKIN_FIELDS：8 个配色键契约（与 themes.js 皮肤色键一致）', () => {
  assert.equal(SKIN_FIELDS.length, 8);
  const keys = SKIN_FIELDS.map((f) => f.key);
  assert.deepEqual(keys.sort(), ['accentA', 'accentB', 'cardBg', 'cream', 'creamBorder', 'creamText', 'ink', 'pageBg']);
  for (const f of SKIN_FIELDS) assert.ok(f.label && f.hint, `${f.key} 缺 label/hint`);
});

test('normalizeSkin：合法 hex 保留（3位/6位/大小写），非法值丢弃', () => {
  const out = normalizeSkin({
    pageBg: '#F4FAFD', accentA: '#abc', accentB: '#53DE7B',
    ink: 'red', cardBg: 'ffffff', cream: '#EFE6D4',
    creamBorder: '#DBCBB0', creamText: '#9A8B72',
  });
  assert.equal(out.pageBg, '#F4FAFD', '6位hex保留');
  assert.equal(out.accentA, '#abc', '3位hex保留');
  assert.equal(out.ink, undefined, '颜色名丢弃');
  assert.equal(out.cardBg, undefined, '缺#号丢弃');
  assert.equal(out.cream, '#EFE6D4');
});

test('normalizeSkin：未知键过滤、null/非对象容错返回空对象', () => {
  assert.deepEqual(normalizeSkin({ pageBg: '#fff', radius: 99, foo: '#123' }), { pageBg: '#fff' });
  assert.deepEqual(normalizeSkin(null), {});
  assert.deepEqual(normalizeSkin('不是对象'), {});
  assert.deepEqual(normalizeSkin([]), {});
});
```

- [ ] **Step 2: 运行确认失败**

```bash
cd frontend; node --test tests/skin.test.mjs
```
Expected: FAIL（Cannot find module '../src/utils/skin.js'）

- [ ] **Step 3: 实现 skin.js**

创建 `frontend/src/utils/skin.js`：

```js
// AI 生成皮肤（B 批）：配色契约与清洗纯函数
// 契约 = themes.js 皮肤对象的 8 个色键；AI 只生成配色，圆角/字号等数值令牌仍由用户手调

// 字段清单：key/label/hint 与后端 gen_skin prompt 语义一一对应（prompt 见 prompts.mjs）
export const SKIN_FIELDS = [
  { key: 'pageBg', label: '页面底色', hint: '浅色，整页背景' },
  { key: 'accentA', label: '强调色A', hint: '标题错位层/序号胶囊/金句描边' },
  { key: 'accentB', label: '强调色B', hint: '标签胶囊/圆点/菱形装饰' },
  { key: 'ink', label: '正文墨色', hint: '深色，正文文字与卡片描边' },
  { key: 'cardBg', label: '卡片底色', hint: '接近白色' },
  { key: 'cream', label: '落款底色', hint: '浅色，落款卡背景' },
  { key: 'creamBorder', label: '落款描边', hint: '比落款底色深一档' },
  { key: 'creamText', label: '落款文字', hint: '灰色调' },
];

// 合法 hex：#rgb 或 #rrggbb（大小写均可）
const HEX_RE = /^#([0-9a-fA-F]{3}|[0-9a-fA-F]{6})$/;

// 清洗 AI 输出：只保留 8 个契约键中的合法 hex；任何异常输入返回空对象（调用方据此报错）
export function normalizeSkin(raw) {
  if (!raw || typeof raw !== 'object' || Array.isArray(raw)) return {};
  const out = {};
  for (const { key } of SKIN_FIELDS) {
    const v = raw[key];
    if (typeof v === 'string' && HEX_RE.test(v.trim())) out[key] = v.trim();
  }
  return out;
}
```

- [ ] **Step 4: 运行测试通过**

```bash
cd frontend; node --test tests/skin.test.mjs
```
Expected: 3 tests PASS

- [ ] **Step 5: Commit**

```bash
git add frontend/src/utils/skin.js frontend/tests/skin.test.mjs
git commit -m "feat(AI皮肤): skin.js 配色契约+normalizeSkin 清洗（TDD 3单测，AI输出防线）"
```

---

## Task 2: 后端 gen_skin action（TDD）

**Files:**
- Modify: `backend/functions/lib/prompts.mjs`（PROMPTS 字典末尾追加）
- Create: `backend/functions/tests/gen-skin.test.mjs`

**Interfaces:**
- Consumes: 无
- Produces: `PROMPTS.gen_skin(p)`，`p = { text }`（风格描述），返回 `[{role:'system'},{role:'user'}]`；ai.mjs 零改动自动分发。Task 3 前端 `callAI('gen_skin', { text })` 依赖此 action 名

- [ ] **Step 1: 写失败测试**

创建 `backend/functions/tests/gen-skin.test.mjs`：

```js
// gen_skin prompt 契约测试：action 存在、双消息结构、8 色键字段与 SKIN_FIELDS 语义对齐
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { PROMPTS } from '../lib/prompts.mjs';

test('gen_skin：存在且返回 system+user 双消息', () => {
  assert.ok(PROMPTS.gen_skin, 'gen_skin action 未定义');
  const msgs = PROMPTS.gen_skin({ text: '蓝金科技感' });
  assert.equal(msgs.length, 2);
  assert.equal(msgs[0].role, 'system');
  assert.equal(msgs[1].role, 'user');
  assert.ok(msgs[1].content.includes('蓝金科技感'), '用户描述要进 prompt');
});

test('gen_skin：prompt 含全部 8 个色键名与严格 JSON 输出要求', () => {
  const c = PROMPTS.gen_skin({ text: 'x' })[1].content;
  for (const k of ['pageBg', 'accentA', 'accentB', 'ink', 'cardBg', 'cream', 'creamBorder', 'creamText']) {
    assert.ok(c.includes(k), `prompt 缺字段说明 ${k}`);
  }
  assert.ok(c.includes('JSON'), '要求 JSON 输出');
  assert.ok(c.includes('hex'), '要求 hex 格式');
});
```

- [ ] **Step 2: 运行确认失败**

```bash
cd backend/functions; node --test tests/gen-skin.test.mjs
```
Expected: FAIL（gen_skin action 未定义，第一个断言失败）

- [ ] **Step 3: prompts.mjs 字典末尾追加（organize_review_notes 之后）**

```js
  // AI 生成皮肤配色（B 批）：风格描述 → 8 个 hex（前端 skin.js normalizeSkin 清洗后应用）
  gen_skin: (p) => [
    { role: 'system', content: '你是公众号排版配色设计师，擅长把抽象的风格描述转化为和谐、对比度适宜的配色方案。' },
    {
      role: 'user',
      content: `为推文排版模板生成一套配色。风格描述：${p.text || '清新活泼的校园风'}

输出以下 8 个字段的 hex 颜色值（#rrggbb 格式），各字段用途与要求：
- pageBg：页面底色，必须浅色（如 #F7F5F0）
- accentA：强调色A，用于标题装饰层/序号/金句描边，饱和度可高
- accentB：强调色B，用于标签/圆点装饰，与 accentA 和谐但不相同
- ink：正文与描边墨色，必须深色保证可读（如 #3E3E3E）
- cardBg：卡片底色，接近白色（如 #ffffff）
- cream：落款卡底色，浅色
- creamBorder：落款卡描边，比 cream 深一档
- creamText：落款卡文字，灰色调

要求：整体和谐、手机阅读对比度足够、贴合风格描述的色感。
严格按 JSON 对象输出，不要任何其他文字，不要 markdown 代码块包裹，如：
{"pageBg":"#...","accentA":"#...","accentB":"#...","ink":"#...","cardBg":"#...","cream":"#...","creamBorder":"#...","creamText":"#..."}`,
    },
  ],
```

- [ ] **Step 4: 运行测试通过**

```bash
cd backend/functions; node --test tests/gen-skin.test.mjs
```
Expected: 2 tests PASS。再跑后端全量 `npm test`（既有 3+4 项 + 新 2 项全绿）

- [ ] **Step 5: Commit**

```bash
git add backend/functions/lib/prompts.mjs backend/functions/tests/gen-skin.test.mjs
git commit -m "feat(AI皮肤): 后端 gen_skin action——风格描述→8色hex严格JSON（字典分发零改ai.mjs）"
```

---

## Task 3: TaskDetail「✨ AI 配色」弹窗 + 应用链路

**Files:**
- Modify: `frontend/src/components/TaskDetail.vue`

**Interfaces:**
- Consumes: `normalizeSkin`（Task 1）；`callAI('gen_skin', { text })`（Task 2）；既有 `themeOverrides`/`themeSnapshot` watch/防抖保存链路
- Produces: 无新接口（UI 功能完整交付）

- [ ] **Step 1: script 三处新增（串行 Edit，禁止并行）**

1a. 导入区（`import ThemeGallery` 行后）加：

```js
import { normalizeSkin } from '../utils/skin.js'; // AI 皮肤输出清洗（B 批）
```

1b. 主题区（`const galleryOpen = ref(false);` 行后）加：

```js
// AI 生成皮肤弹窗（B 批）：输入风格描述 → gen_skin → 清洗 → 应用为当前任务覆盖
const skinModal = reactive({ show: false, input: '', loading: false });
function openSkinModal() {
  skinModal.show = true;
  skinModal.input = '';
}
// 生成并应用：AI 输出经 normalizeSkin 清洗（字段过滤+hex校验），合法键数<8 视为失败提示重试
async function generateSkin() {
  if (!skinModal.input.trim()) return;
  skinModal.loading = true;
  error.value = '';
  try {
    const text = await callAI('gen_skin', { text: skinModal.input.trim() });
    const clean = text.replace(/```json\s*/g, '').replace(/```\s*/g, '').trim();
    const skin = normalizeSkin(JSON.parse(clean));
    if (Object.keys(skin).length < 8) {
      error.value = 'AI 生成的配色不完整，请换个描述重试（或用「🎨 调参数」手动配色）';
      return;
    }
    // 应用 = 清空旧覆盖再写入整套配色（AI 皮肤是完整方案，同切预设清覆盖语义）
    for (const k of Object.keys(themeOverrides)) delete themeOverrides[k];
    Object.assign(themeOverrides, skin);
    skinModal.show = false; // 关弹窗，预览即时刷新+防抖自动保存（既有链路）
  } catch (e) {
    error.value = 'AI 生成失败，请重试：' + e.message;
  } finally {
    skinModal.loading = false;
  }
}
```

- [ ] **Step 2: 模板两处新增（串行 Edit）**

2a. 预览头部「🖼 画廊」按钮之后、「🎨 调参数」之前加：

```html
          <button class="param-toggle" @click="openSkinModal" title="AI 按描述生成配色">
            ✨ AI 配色
          </button>
```

2b. 末尾弹窗区（ThemeGallery 挂载之后）加：

```html
    <!-- AI 配色弹窗：风格描述 → 整套配色应用（复用 modal-mask/modal 既有样式） -->
    <div v-if="skinModal.show" class="modal-mask" @click.self="skinModal.show = false">
      <div class="modal">
        <p class="modal-title">✨ AI 生成配色</p>
        <textarea v-model="skinModal.input" rows="3"
          placeholder="描述想要的风格，如：蓝金科技感 / 温柔奶油风 / 圣诞红绿"></textarea>
        <div class="modal-btns">
          <button @click="skinModal.show = false">取消</button>
          <button class="primary" :disabled="skinModal.loading || !skinModal.input.trim()" @click="generateSkin">
            {{ skinModal.loading ? '生成中…' : '生成并应用' }}
          </button>
        </div>
        <p class="skin-tip">生成的是整套配色（底色/强调色/文字色等 8 项）；圆角字号等细调用「🎨 调参数」</p>
      </div>
    </div>
```

样式（scoped style 末尾追加）：

```css
/* AI 配色弹窗提示行（B 批） */
.skin-tip { font-size: 12px; color: #999; margin: 8px 0 0; }
```

- [ ] **Step 3: 构建 + 全量测试**

```bash
cd frontend; npm run build
```
Expected: `✓ built in`（忽略沙箱报错）

```bash
cd frontend; npm run test
```
Expected: 18 项全 PASS（skin 3 + 既有 15）

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/TaskDetail.vue
git commit -m "feat(AI皮肤): 详情页 AI 配色弹窗——描述→清洗→应用，复用预览/自动保存/持久化链路"
```

---

## Task 4: 主会话浏览器实测 + 部署 + 线上验收（主会话执行，不派子代理）

- [ ] **Step 1: 本地双服务（dev-server.mjs 8888 + vite 5173）+ Playwright 实测**

实测清单（脚本模式，参照批 1 的 test_gallery.py）：
1. 打开任务 → 预览头部出现「✨ AI 配色」按钮
2. 点击 → 弹窗 → 输入「蓝金科技感」→ 生成并应用
3. 等待 AI 返回（P2-7 进度提示超 8s 出现）→ 弹窗关闭 → 预览底色/强调色变为蓝金系（断言 preview HTML 含 AI 返回的 pageBg hex）
4. 「🎨 调参数」面板中 accentA/accentB 色板值 = AI 生成值（覆盖已写入）
5. 刷新页面 → 配色保持（layout_theme 持久化）
6. 回归：画廊/复制/参考文献正常；失败路径（输入乱码描述）给出可读错误

- [ ] **Step 2: 还原测试任务数据（service key 清 content/layout_theme）**

- [ ] **Step 3: push 部署 + 线上冒烟（Playwright 对 netlify 站点跑同一脚本）**

```bash
git push origin master
```

- [ ] **Step 4: 用户线上验收**

1. 任意任务 → 「✨ AI 配色」→ 输入风格词 → 应用 → 预览变装
2. 刷新保持；「🎨 调参数」可继续微调；切预设恢复预设配色
3. 回归红线：复制到公众号/旧任务/口令门禁正常

---

## Self-Review 记录

- 规格覆盖：契约+清洗→Task 1；AI action→Task 2；UI+应用链路→Task 3；实测部署验收→Task 4 ✓
- 占位符扫描：无 TBD/TODO，全部代码完整 ✓
- 类型一致性：`SKIN_FIELDS[].{key,label,hint}` 与 Task 2 prompt 字段、Task 1 测试键名一致；`normalizeSkin(raw)→object` 与 Task 3 调用一致；`callAI('gen_skin',{text})` 与 Task 2 `p.text` 一致 ✓
- 风险与兜底：AI 输出非 JSON/缺字段 → JSON.parse 抛错或 <8 键 → 可读错误提示；hex 非法 → normalizeSkin 丢弃 → <8 键同样报错重试；数值令牌不交给 AI（红线 2），无破坏性布局风险 ✓
