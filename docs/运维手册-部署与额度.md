# 运维手册：部署、额度与迁移预案

> 适用阶段：内测期（2026-09 起）。本文档面向维护者，第 5 节可直接转发社团成员。

## 0. 当前生产环境（2026-09-06 迁移后）

| 项 | 值 |
|---|---|
| **生产地址** | **https://tuiwen-ehc.pages.dev**（Cloudflare Pages，项目名 tuiwen） |
| 部署方式 | **wrangler 直传**：根目录执行 `npx wrangler pages deploy frontend/dist --project-name=tuiwen --branch=master` |
| 适配层 | `functions/api/[[path]].js`（/api/* 分发 + process.env 垫片），`frontend/public/_routes.json`（仅 /api/* 走函数） |
| 数据库/图片 | Supabase（项目 mnzjyiophdogjjffifyl，与托管平台无关，不受影响） |
| 密钥 | Cloudflare Pages secrets（已配置 8 项）；本地 .env.local（已 gitignore）；wrangler 本地测试用 .dev.vars（已 gitignore） |
| Netlify 旧站 | https://timely-rugelach-be68fd.netlify.app 保留为备用（额度耗尽会暂停），其域名/配置不动 |
| 日常部署 | 本地 `npm run build`（frontend 下）通过后执行上面 wrangler 命令；**不再依赖 push 触发构建** |

⚠️ 注意：`tuiwen.netlify.app` / `tuwen.netlify.app` 是重名的陌生站点，与本项目无关。
⚠️ git push 到 GitHub 仍会触发 Netlify 构建（若其额度已耗尽则构建失败，免费无损失）。

### wrangler 本地开发/验证
- 登录状态：已完成 OAuth（13923075709@163.com），凭据存于本机 `~/.wrangler` / `%USERPROFILE%\.wrangler`
- 本地运行时验证：`npx wrangler pages dev frontend/dist --port 8788 --compatibility-flags=nodejs_compat`（密钥读 .dev.vars）
- 修改 secrets：`npx wrangler pages secret put <KEY> --project-name=tuiwen`（stdin 传值），改完需重新 deploy 生效

## 1. Netlify 免费额度现状（2026-09-06，已降级为备用）

- 免费计划 300 credits/月，全池共享：生产部署 15/次、带宽 20/GB、请求 2/万次、Functions 计算 10/GB-hour
- **当前余额约 30 credits** ≈ 再部署 2 次，或约 1.5GB 流量（含 AI 功能调用的 Functions 计算）
- 耗尽后果：全站暂停显示 "Site not available"，直到下个账单周期自动重置（恢复 300）；不产生账单，数据不丢
- 邮件提醒阈值：50% / 75% / 100%

## 2. 部署纪律（当前阶段执行）

1. 部署走 **wrangler 直传**（不再依赖 git push 触发），无额度消耗担忧；但仍保持"本地 build+测试通过后再部署"的习惯
2. git push 到 GitHub 仅作代码备份（会触发 Netlify 构建：额度尽则失败免费，未尽则扣 15）

## 3. 迁移记录与预案

### Cloudflare Pages 迁移（✅ 2026-09-06 已完成）
- 适配层：`functions/api/[[path]].js` + `frontend/public/_routes.json`
- 本地验证：wrangler pages dev（nodejs_compat）8 项路由 + 上传/删除全链路通过
- 线上验证：口令门 401 / 任务列表 200 / 图片 API 200 / SPA 回退 200
- Git 集成尝试：GitHub App 已安装（Chim6688 账号，installation 159419038，仅授权 campus-media-hub），但 OAuth 回跳循环未通——采用 wrangler 直传替代；日后如需 Git 集成自动构建可再排查

### 国内云 + ICP 备案（正式面向公众运营时才启动）
- 备案法定前提 = 服务器在中国大陆；**Cloudflare 与备案无关**（其"中国网络"是企业版付费功能，且要求先有备案才能开通）
- 主体问题：学生社团非法人，备案需指导老师个人名义或挂靠学校——**先问学校信息化办公室**走校内流程
- 届时迁移目标：腾讯云/阿里云 + 备案 + 域名

## 4. 给社团成员的说明（可直接转发）

> 站点已迁移至 Cloudflare Pages（https://tuiwen-ehc.pages.dev），免费额度充足，正常情况下不会再出现访问暂停。请收藏新地址；若遇到个别网络环境打不开，稍后重试即可。稿件与数据存储在独立数据库（Supabase），不会因平台问题丢失。
